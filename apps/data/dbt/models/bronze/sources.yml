version: 2

sources:
  - name: bronze
    description: Raw tables loaded by dlt into the bronze schema.
    schema: bronze
    tables:
      - name: smoke_events
        description: F2-10 smoke table loaded by the Dagster/dlt scaffold asset.
      - name: production_by_well
        description: Raw monthly production-by-well snapshot loaded from datos.gob.ar.
      - name: wells_registry
        description: Raw complementary wells registry snapshot loaded from datos.gob.ar.
