# Copy this file to backend.config and fill in values from: make bootstrap
# backend.config is gitignored — never commit it.
bucket         = "petrocast-tf-state-XXXXXXXX"
region         = "us-east-1"
dynamodb_table = "petrocast-tf-locks"
encrypt        = true
