# Copy to terraform.tfvars (gitignored) and fill in your values.
# state_bucket: from `make output-bootstrap` → state_bucket output
state_bucket       = "petrocast-tf-state-XXXXXXXX"
traefik_acme_email = "your@email.com"
